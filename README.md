

Learning Journal Entry #8


1.  A regular Excel table holds raw, row-by-row data, while a pivot table summarizes that data dynamically to show totals, counts, and averages.


2.  I would choose a pivot table for quick visual data exploration in Excel, but I would use SQL `GROUP BY` when handling massive datasets stored in an external database.


3.  Pivot Table 5 was the trickiest to build because I had to right-click the dates and manually apply the grouping feature to roll them up into years.


4.  Yes, my pivot table headcounts and totals matched my SQL query results perfectly once I correctly adjusted the fields to count instead of sum.


5.  Tomorrow, I could use a pivot table to analyze sales performance by tracking which product category generates the highest total revenue across different store locations.
